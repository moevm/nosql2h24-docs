import React, {useEffect, useState} from 'react'
import VersionList from '../components/VersionList';

function VersionListPage() {
    return (
        <div className="VersionListPage">
          <VersionList/>
        </div>
      );
}

export default VersionListPage;